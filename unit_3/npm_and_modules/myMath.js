const sum = (a, b) => a + b;

const multiply = (a, b) => a * b;

const power = (base, exponent) => {
  return Math.pow(base, exponent);
}

const myMagicNumber = 3.1416;

module.exports = {
  sum,
  multiply,
  power,
  myMagicNumber
}
